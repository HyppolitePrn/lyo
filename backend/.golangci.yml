version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - noctx
    - exhaustive

linters-settings:
  govet:
    enable-all: true

issues:
  max-issues-per-linter: 50
  max-same-issues: 3
