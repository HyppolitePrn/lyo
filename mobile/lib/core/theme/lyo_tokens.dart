import 'package:flutter/material.dart';

// ── Colors — Dark ─────────────────────────────────────────────────────────────
const Color lyoBgDark = Color(0xFF131110);
const Color lyoSurfaceDark = Color(0xFF1E1C1A);
const Color lyoSurface2Dark = Color(0xFF272422);
const Color lyoNavDark = Color(0xFF181614);
const Color lyoBorderDark = Color(0xFF2A2724);
const Color lyoTextDark = Color(0xFFF5F3F1);
const Color lyoSubDark = Color(0xFF8A837E);

// ── Colors — Light ────────────────────────────────────────────────────────────
const Color lyoBgLight = Color(0xFFFDFCFB);
const Color lyoSurfaceLight = Color(0xFFF3F1EE);
const Color lyoSurface2Light = Color(0xFFEAE7E3);
const Color lyoNavLight = Color(0xFFFDFCFB);
const Color lyoBorderLight = Color(0xFFE2DDD9);
const Color lyoTextLight = Color(0xFF1A1614);
const Color lyoSubLight = Color(0xFF7A736E);

// ── Accent ────────────────────────────────────────────────────────────────────
const Color lyoAccent = Color(0xFFE8B84A);
const Color lyoError = Color(0xFFE05A5A);

// ── Typography scale ──────────────────────────────────────────────────────────
const double lyoDisplay = 32.0;
const double lyoH1 = 22.0;
const double lyoH2 = 20.0;
const double lyoBody1 = 15.0;
const double lyoBody2 = 14.0;
const double lyoCaption = 13.0;
const double lyoSmall = 11.0;
const double lyoTiny = 10.0;

// ── Spacing ───────────────────────────────────────────────────────────────────
const double lyoPadH = 32.0;
const double lyoPadHMain = 20.0;
const double lyoGapXS = 4.0;
const double lyoGapS = 8.0;
const double lyoGapM = 12.0;
const double lyoGapL = 16.0;
const double lyoGapXL = 24.0;
const double lyoGapXXL = 32.0;
const double lyoGapXXXL = 40.0;

// ── Border radii ──────────────────────────────────────────────────────────────
const double lyoRadiusBtn = 16.0;
const double lyoRadiusCard = 16.0;
const double lyoRadiusInput = 14.0;
const double lyoRadiusChip = 10.0;
const double lyoRadiusFull = 999.0;

// ── Shadows ───────────────────────────────────────────────────────────────────
const BoxShadow lyoCtaGlow = BoxShadow(
  color: Color(0x55E8B84A),
  blurRadius: 24,
  offset: Offset(0, 8),
);

const BoxShadow lyoMiniPlayerShadow = BoxShadow(
  color: Color(0x66000000),
  blurRadius: 32,
  offset: Offset(0, 8),
);

const BoxShadow lyoArtworkShadow = BoxShadow(
  color: Color(0x33E8B84A),
  blurRadius: 60,
  offset: Offset(0, 24),
);
